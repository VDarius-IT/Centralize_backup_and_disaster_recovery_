groups:
  - name: backup.rules
    rules:
      - alert: BackupJobFailed
        expr: backup_job_failed_total > 0
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "One or more backup jobs failed"
