# AWS
AWS_REGION=us-east-1
AWS_PROFILE=

# Backup repository (S3) - if using local mode, leave blank
BACKUP_BUCKET=

# Local backup root (for simulated/testing)
LOCAL_BACKUP_ROOT=./backups

# Retention policy
RETENTION_DAYS=30

# Monitoring
METRICS_LISTEN_ADDR=0.0.0.0
METRICS_LISTEN_PORT=8000
